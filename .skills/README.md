# Shared project skills

Skills here are used by **Cursor** (`.cursor/skills` → this folder) and **Claude Code** (`.claude/skills` → this folder).

Add new skills as `.skills/<skill-name>/SKILL.md` only—do not edit under `.cursor/` or `.claude/` directly.
